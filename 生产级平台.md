# 生产级平台.md

## 0. 文档目标与使用方式
本文档是当前灾害监测平台从“可运行雏形”升级到“生产级平台”的工程实施书。

使用原则：
- 后续所有升级按本文档阶段顺序推进。
- 每个阶段都要完成“设计、实施、验证、文档、回滚”五件事。
- 每一项改造都必须有明确验收指令，避免只做代码、不做闭环。

本文档回答四个核心问题：
- 现在系统做到哪一步了
- 还缺哪些生产能力
- 每一步应该怎么改
- 每一步改完如何验收

---

## 1. 当前基线

### 1.1 已完成能力
当前项目已经完成以下基础能力：
- MQTT 三类业务主题接入：`classification`、`speed`、`device_status`
- MySQL 业务表落地：`disaster_data`、`speed_data`、`device_location`、`device_status`
- FastAPI 查询接口与时间范围过滤
- API Key 鉴权与基础最小权限控制
- 入参校验、脏数据拦截、脏数据测试脚本
- 索引复核与查询验证
- Nginx HTTPS 网关
- 多环境配置：`dev/test/prod`
- 基础可观测性：结构化日志、`/metrics`、健康检查
- 备份恢复与恢复演练验证

### 1.2 当前部署形态
当前已经具备一套“可部署、可验证、可演练”的单机版生产化基础架构：
- Nginx：统一入口、HTTPS、反向代理
- Backend：FastAPI + MQTT 消费 + MySQL 入库
- MQTT Broker：Mosquitto
- MySQL：容器化数据库，端口映射到 `3307`
- 脚本体系：启动、停止、校验、验收、备份、恢复、测试

### 1.3 当前定位
当前系统已经达到：
- 可部署到测试服务器
- 可进行小规模试运行
- 具备生产化第一阶段基础能力

当前系统尚未完全达到：
- 正式生产级高可用
- 大规模并发设备接入
- 完整运维监控告警闭环
- 多节点容灾与平台化运营

---

## 2. 总体阶段规划

### 2.1 P0：生产可用底座
目标：先把系统做成“能安全上线、能发现问题、能恢复数据”的版本。

范围：
- P0-1 网关与 HTTPS
- P0-2 鉴权与密钥治理
- P0-3 配置分环境
- P0-4 监控与日志
- P0-5 备份与恢复

### 2.2 P1：业务闭环稳定化
目标：让平台具备真正的灾害业务闭环能力，而不是只做数据接收和查询。

范围：
- P1-1 告警规则引擎
- P1-2 通知通道
- P1-3 审计与追溯
- P1-4 数据生命周期治理
- P1-5 接口治理与服务保护

### 2.3 P2：高可用与平台化
目标：让系统具备规模化部署、持续交付和多角色运维能力。

范围：
- P2-1 后端高可用
- P2-2 MQTT 集群化
- P2-3 MySQL 高可用
- P2-4 设备管理中心
- P2-5 可视化平台与报表
- P2-6 CI/CD 与灰度发布

---

## 3. 执行规则

### 3.1 每阶段固定输出
每完成一个子阶段，必须交付：
- 代码改动
- 配置文件
- 启停脚本
- 验收脚本
- 验收结果
- 回滚说明

### 3.2 每阶段固定验收证据
必须至少提供三类证据：
- 功能证据：接口结果、数据库结果、消息流结果
- 稳定性证据：日志、指标、监控结果
- 安全性证据：鉴权、权限、错误路径验证

### 3.3 执行顺序
每个阶段按以下顺序推进：
1. 先补文档与设计边界
2. 再做代码和脚本改造
3. 再做验收脚本
4. 再做人工验收
5. 最后更新本文档状态

---

## 4. P0 详细实施

## 4.1 P0-1 网关与 HTTPS

### 目标
建立统一安全入口，外部只通过 Nginx 访问，默认使用 HTTPS。

### 需要解决的问题
- 后端服务不应直接裸露给外部
- HTTP 明文传输不适合生产环境
- 需要统一的入口层承载安全头、反代和限流能力

### 改造项
- 引入 Nginx 容器
- 建立 HTTP 到 HTTPS 跳转
- 配置 TLS 证书
- 反代 `/health`、`/api/*`
- 为未来限流与安全头预留位置

### 已完成结果
- 已完成 Nginx 接入
- 已完成 HTTPS 自签证书流程
- 已完成 HTTP 跳转 HTTPS
- 已完成接口经 HTTPS 正常访问验证

### 验收指令
```powershell
powershell -ExecutionPolicy Bypass -File .\test\verify_p0_1_https.ps1
```

### 验收标准
- `http://127.0.0.1/health` 返回 301 跳转
- `https://127.0.0.1/health` 返回 200
- 不带鉴权访问 `/api/*` 返回 401
- 带鉴权访问 `/api/*` 返回 200

### 回滚点
- 保留 `http://127.0.0.1:8000` 直连能力
- Nginx 出现问题可临时绕过网关直连后端

---

## 4.2 P0-2 鉴权与密钥治理

### 目标
把鉴权从“单个写死 key”提升到“可轮换、可失效、可审计”的模式。

### 需要解决的问题
- API Key 不能长期固定不变
- 需要支持主 Key、备用 Key、失效 Key
- 鉴权成功和失败都要可追踪

### 改造项
- 支持 `API_KEY`
- 支持 `API_KEYS`
- 支持 `API_KEYS_DISABLED`
- 增加密钥轮换脚本
- 增加密钥失效脚本
- 增加认证日志

### 已完成结果
- 已实现多 Key 鉴权
- 已实现轮换与失效脚本
- 已实现鉴权日志记录
- 已完成缺失、错误、主 Key、备 Key、失效 Key 验证

### 验收指令
```powershell
powershell -ExecutionPolicy Bypass -File .\test\verify_p0_2_auth.ps1
```

轮换验证：
```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\rotate_api_key.ps1 -RetainOldCount 1
```

失效验证：
```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\disable_api_key.ps1 -Key "<旧Key>"
```

### 验收标准
- 无认证头返回 401
- 错误 Key 返回 401
- 主 Key 返回 200
- 备 Key 在轮换窗口可用
- 失效 Key 被拒绝

### 回滚点
- 保留单 Key 模式
- 多 Key 异常时可退回只读取 `API_KEY`

---

## 4.3 P0-3 配置分环境

### 目标
让 `dev/test/prod` 环境完全隔离，避免串库、串 MQTT、串日志级别。

### 需要解决的问题
- 单一 `.env` 无法满足多环境部署
- 启动脚本要能显式指定环境
- 环境缺项时要拒绝启动

### 改造项
- 新增 `.env.dev`、`.env.test`、`.env.prod`
- 启动脚本支持 `-Env`
- 增加环境配置校验脚本
- 启动时打印当前环境关键信息

### 已完成结果
- 已支持 `-Env dev/test/prod`
- 已完成环境自检
- 已完成不同环境配置读取验证

### 验收指令
```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\check_env.ps1 -EnvFile .env.dev -Mode all
powershell -ExecutionPolicy Bypass -File .\scripts\check_env.ps1 -EnvFile .env.test -Mode all
powershell -ExecutionPolicy Bypass -File .\scripts\check_env.ps1 -EnvFile .env.prod -Mode all
```

```powershell
powershell -ExecutionPolicy Bypass -File .\test\verify_p0_3_env.ps1 -Env dev
powershell -ExecutionPolicy Bypass -File .\test\verify_p0_3_env.ps1 -Env test
powershell -ExecutionPolicy Bypass -File .\test\verify_p0_3_env.ps1 -Env prod
```

### 验收标准
- 三个环境文件校验通过
- 启动时能正确打印所选环境
- DB、MQTT、日志级别随环境变化
- 环境缺失字段时禁止启动

### 回滚点
- 保留单 `.env` 兼容模式

---

## 4.4 P0-4 监控与日志

### 目标
做到线上可发现、可定位、可追踪。

### 需要解决的问题
- 出现异常时不能只看终端打印
- 接口错误和 MQTT 消息处理需要指标化
- 每个请求需要可关联的 request_id

### 改造项
- 结构化 JSON 日志
- 认证日志、请求日志、MQTT 日志
- 暴露 `/metrics`
- 增加 Prometheus 指标
- 记录 4xx/5xx、MQTT 连接状态、MQTT 消息计数

### 已完成结果
- 已接入 `prometheus-client`
- 已提供 `/metrics`
- 已完成 request_id 中间件
- 已完成 JSON 日志输出
- 已完成 MQTT 连接与消息指标验证

### 验收指令
```powershell
powershell -ExecutionPolicy Bypass -File .\test\verify_p0_4_observability.ps1
```

### 验收标准
- `/health` 正常
- 401 与 200 都能触发日志与指标
- `/metrics` 中能看到 HTTP 和 MQTT 指标
- 日志中可按 `request_id` 追踪请求

### 回滚点
- 指标和日志增强失败不能影响主业务
- `/metrics` 不可用时主接口仍应正常工作

---

## 4.5 P0-5 备份与恢复

### 目标
做到“数据可备份、可恢复、可演练”。

### 需要解决的问题
- 不能只有数据，没有恢复验证
- 需要支持演练库恢复
- 需要明确业务账号与管理员账号分工

### 改造项
- 备份脚本：`scripts/backup_db.ps1`
- 恢复脚本：`scripts/restore_db.ps1`
- 恢复演练脚本：`test/verify_p0_5_backup_restore.ps1`
- 备份元数据记录
- Drill 库恢复与计数比对

### 已完成结果
- 已修复备份输出解析问题
- 已修复容器 MySQL root 密码差异问题
- 已修复恢复导入路径问题
- 已修复 Drill 库权限授予问题
- 已完成备份恢复演练验证

### 当前使用约定
- 业务账号：`Evil_Pidan_King`
- 业务账号用途：后端运行、日常查询、普通读写
- 管理员账号：`root`
- 管理员账号用途：建库、恢复、授权、灾备演练

### 验收指令
```powershell
powershell -ExecutionPolicy Bypass -File .\test\verify_p0_5_backup_restore.ps1 -Env dev -AdminUser root -AdminPassword root123
```

### 验收标准
- 成功生成 `.sql` 与 `.meta.txt`
- 成功恢复到 `mf_monitor_drill`
- 原库与 Drill 库计数一致
- 恢复流程可以重复执行

### 回滚点
- 保留备份文件
- Drill 库可单独删除，不影响主库

---

## 5. P1 详细规划

## 5.1 P1-1 告警规则引擎

### 目标
从“存数据”升级到“识别风险并触发告警”。

### 需要新增的核心能力
- 告警规则表
- 告警事件表
- 告警状态机
- 去重、抑制、升级策略

### 建议规则样例
- `disaster_type='flood'` 且 `confidence>=0.90` 触发高优先级告警
- `speed` 连续 N 次超过阈值触发滑坡风险告警
- `device_status='off'` 持续超过阈值触发离线告警

### 交付物
- 告警数据表
- 规则配置表
- 规则执行服务
- 验收脚本
- 回滚说明

---

## 5.2 P1-2 通知通道

### 目标
把告警从系统内部状态，变成真正能触达人的信息。

### 需要新增的核心能力
- 邮件通知
- 企业微信/钉钉/飞书 Webhook
- 短信或第三方短信平台适配
- 通知模板
- 重试与失败补偿

### 验收目标
- 告警生成后可触发至少 1 种外部通知
- 通知失败有重试和日志记录

---

## 5.3 P1-3 审计与追溯

### 目标
让关键操作可以追踪到“谁、何时、做了什么”。

### 需要新增的核心能力
- API 访问审计
- 配置变更审计
- 告警确认/关闭审计
- 管理操作审计

### 验收目标
- 任意关键操作都能查到审计记录
- 审计记录不可被普通业务路径覆盖

---

## 5.4 P1-4 数据生命周期治理

### 目标
控制数据库长期运行成本，避免表无限增长。

### 需要新增的核心能力
- 热数据保留策略
- 冷数据归档策略
- 历史数据清理任务
- 月表或分区规划

### 验收目标
- 可以按时间范围归档旧数据
- 清理策略不影响在线查询

---

## 5.5 P1-5 接口治理

### 目标
让 API 更适合外部长期使用和平台集成。

### 需要新增的核心能力
- 分页
- 限流
- 幂等保护
- 统一错误码
- 版本化预留

### 验收目标
- 查询接口支持分页
- 高频恶意调用可被限制
- 写接口具备幂等保护方案

---

## 6. P2 详细规划

## 6.1 P2-1 后端高可用
- 多实例部署
- 网关健康检查
- 无状态服务设计
- 异常实例自动摘除

## 6.2 P2-2 MQTT 集群化
- 评估 EMQX 或桥接集群方案
- 解决单节点 Broker 风险
- 引入连接数、吞吐量、堆积监控

## 6.3 P2-3 MySQL 高可用
- 主从复制
- 只读副本
- 故障切换策略
- 备份与高可用协同

## 6.4 P2-4 设备管理中心
- 设备注册
- 设备分组
- 在线状态总览
- 参数配置下发
- 设备权限控制

## 6.5 P2-5 可视化平台
- 地图定位
- 实时态势展示
- 告警看板
- 历史趋势分析
- 报表导出

## 6.6 P2-6 CI/CD 与发布体系
- 自动测试
- 自动构建
- 自动部署
- 灰度发布
- 快速回滚

---

## 7. 服务器部署前检查清单

正式部署到服务器前，至少完成以下检查：
- 生产域名和正式证书
- 生产 `.env.prod` 核对
- MySQL root、业务账号、API Key 已分权
- Nginx、Backend、MQTT、MySQL 启停流程可复现
- 备份恢复演练已通过
- 关键指标和日志可观测
- 防火墙与端口策略明确
- 服务器重启后服务可自启动

---

## 8. 当前执行状态

### 8.1 已完成
- [x] 入参校验与约束
- [x] speed/device_status 验证脚本
- [x] 最小权限与 API Key 鉴权
- [x] 数据库索引复核
- [x] 可交付基线文档
- [x] P0-1 网关与 HTTPS
- [x] P0-2 鉴权与密钥治理
- [x] P0-3 配置分环境
- [x] P0-4 监控与日志
- [x] P0-5 备份与恢复
- [x] P2-1 后端高可用
- [x] P2-2 MQTT 集群化

### 8.2 未完成
- [x] P1-1 告警规则引擎
- [x] P1-2 通知通道
- [x] P1-3 审计与追溯
- [x] P1-4 数据生命周期治理
- [x] P1-5 接口治理
- [x] P2-3 MySQL 高可用
- [x] P2-4 设备管理中心
- [x] P2-5 可视化平台
- [x] P2-6 CI/CD 与灰度发布

---

## 9. 后续推进原则

后续建议严格按以下顺序推进：
1. 先完成 P1-1 告警规则引擎
2. 再完成 P1-2 通知通道
3. 然后补 P1-3 到 P1-5，形成完整业务闭环
4. 最后进入 P2，做高可用和平台化

原因：
- P0 解决的是“能上线”
- P1 解决的是“能形成灾害业务闭环”
- P2 解决的是“能长期、大规模、稳定运营”

当前最合适的下一步是：`前端平台化优化（在现有生产级后端基础上继续增强）`














